{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal capability for the main window. Add plugin permissions only as stages need them.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
